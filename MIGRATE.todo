Add user_id to tags :

---------------------------
ALTER TABLE `bp_post_tag` ADD `user_id` VARCHAR( 64 ) NOT NULL ,
ADD `created` DATETIME NOT NULL
---------------------------

Add to feed if it can be commented : 

---------------------------
ALTER TABLE `bp_feed` ADD `feed_comment` SMALLINT NOT NULL
---------------------------

